[package]
name = "libopenv"
version = "0.1.0"
edition = "2024"
description = "OpenV userspace platform library — POSIX adaptation + VFS client"

[dependencies]
openv_syscall = { path = "../openv_syscall" }
vfs-proto = { path = "../vfs-proto" }
